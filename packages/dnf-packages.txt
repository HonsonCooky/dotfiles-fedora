# C/C++
gcc
make

# Go
golang

# Node (nodejs pulls in nodejs-npm via Requires, so npm is not listed separately)
nodejs

# Containers
podman
podman-compose

# System
git
gh
gnome-characters
gnome-tweaks
unzip

# gnome-extensions-cli (installs GNOME extensions from extensions.gnome.org).
# python3-gobject is required; pipx installs the CLI with --system-site-packages
# so the venv can see PyGObject from the system.
pipx
python3-gobject

# Rust compile-time dependencies (ALSA audio, udev gamepad hotplug)
alsa-lib-devel
systemd-devel

# Keymapp dependencies (ZSA keyboard tool)
gtk3
webkit2gtk4.1
libusb1
